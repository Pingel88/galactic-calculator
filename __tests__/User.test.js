import { TestScheduler } from "jest";
import User from "./../src/js/User.js";

describe("User", () => {
  const currentUser = new User();
  test("should create a new instance of the User class");
    expect(currentUser).toEqual(User {});
});